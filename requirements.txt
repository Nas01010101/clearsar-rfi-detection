# ClearSAT-T1 v2 — minimal pinned environment
# Tested on Python 3.11, macOS arm64 (MPS) and Ubuntu 22.04 (CUDA 12.x)

# Core ML
torch>=2.4
torchvision>=0.19
ultralytics>=8.3            # YOLO26x trainer + predictor
pycocotools>=2.0.7          # COCO mAP eval
ensemble-boxes>=1.0.9       # Weighted Box Fusion
sahi>=0.11                  # Sliced inference for small-target detection (Phase B)

# Image / data
opencv-python-headless>=4.10
Pillow>=10.0
numpy>=1.26
pandas>=2.0
tqdm>=4.66

# Optional: run tracking (train_yolo.py logs to local MLflow by default; W&B is opt-in)
mlflow>=2.16
wandb>=0.17

# Optional: H100 / CLAHE prep helpers
scikit-learn>=1.4
scikit-image>=0.24

# NS wild-card (Plan D) deps. mamba-ssm + causal-conv1d are CUDA-only; the NS
# model has a pure-torch fallback for MPS/CPU plumbing tests so these are
# safe to leave uninstalled locally.
pytorch-wavelets>=1.3.0     # differentiable DWT2D for WaveletStem
einops>=0.8.0               # vendored Mamba block
# mamba-ssm>=2.2.2          # uncomment on CUDA host
# causal-conv1d>=1.4.0      # uncomment on CUDA host
